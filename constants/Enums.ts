export enum ICON_TITLES {
  home = 'home',
  translate = 'translate',
  learning = 'study',
  back = 'back',
  camera = 'camera',
  changeCamera = 'camera-change',
  profile = 'profile',
  edit = 'edit',
  chevronRight = 'chevronRight',
  chevronLeft = 'chevronLeft',
  fail = 'fail',
  success = 'success',
  logOut = 'logOut',
  trash = 'trash',
  medium = 'medium',
}

export enum FONT_TYPES {
  light = 'ubuntu-light',
  medium = 'ubuntu-regular',
  regular = 'ubuntu-regular',
  bold = 'ubuntu-bold',
}
